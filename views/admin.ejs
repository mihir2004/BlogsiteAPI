<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog | Database</title>
    <link rel="stylesheet" href="/styles/styles.css">
</head>
<body>
   <div class="container">
    <h1>Tech Blogs</h1>
        <a href="/new" id="newPostButton">New Post</a>
        <a href="/logout" id="logOutButton" class="delete">Logout</a>
        <ul id="postsList">
            <% posts.forEach(post=>{ %>
                <li>
                    <h2>
                        <%= post.title%>

                    </h2>
                    <small>
                        <%= post.created_at%>
                    </small>
                    <p>
                        <%= post.content%>
                    </p>
                    <small>
                        By: <%= post.author%>
                    </small>
                    <a href="/edit/<%=post.id%>" class="edit">Edit</a>
                    <a href="/delete/<%=post.id%>" class="delete">Delete</a>
                </li>
            <%})%>
        </ul>
</div>

    
</body>
</html>